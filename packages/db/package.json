{
  "name": "@simple-coffeeshop/db",
  "version": "1.0.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "generate": "prisma generate",
    "push": "prisma db push && prisma generate",
    "test:push": "NODE_ENV=test prisma db push && NODE_ENV=test prisma generate",
    "test": "NODE_ENV=test vitest run"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.2.0",
    "@prisma/client": "^7.2.0",
    "dotenv": "^17.2.3",
    "dotenv-expand": "^12.0.3",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "@types/pg": "^8.16.0",
    "prisma": "^7.2.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.16"
  }
}
