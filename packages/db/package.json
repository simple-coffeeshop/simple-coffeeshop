{
  "name": "@simple-coffeeshop/db",
  "version": "1.0.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "test": "NODE_ENV=test vitest run",
    "test:push": "NODE_ENV=test prisma db push --env-file ../../.env",
    "generate": "prisma generate",
    "push": "prisma db push"
  },
  "dependencies": {
    "@prisma/client": "^7.2.0"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "prisma": "^7.2.0",
    "typescript": "^5.9.3"
  }
}
