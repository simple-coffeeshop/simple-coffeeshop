{
  "extends": "../config/base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src", // [FIX]: точка, а не ./src
    "types": ["node"]
  },
  "include": ["src"], // [FIX]: добавить tests
  "exclude": ["tests"] // Явно запрещаем трогать тесты при билде
}
