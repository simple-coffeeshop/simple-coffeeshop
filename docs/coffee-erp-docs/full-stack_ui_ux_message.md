Привет! Это Ева. Вот полное Техническое Задание для разработчика, упакованное в Markdown-файл.

Ты можешь просто скопировать этот блок и сохранить как `README.md` или `SPEC.md` для передачи фуллстек-специалисту.

```markdown
# 📂 Техническое Задание: UI/UX Система (Coffee-ERP)

## 1. Дизайн-система (Design Tokens)
Мы используем подход **"Clean SaaS / Glassmorphism"**. Интерфейс должен быть дорогим, воздушным, но при этом функциональным.

### 🎨 Цветовая палитра (CSS Variables)
* **Primary (Акцент):** `#f0533a` (Коралловый).
    * *Использование:* CTA кнопки, активные табы, важные иконки.
* **Backgrounds:**
    * `--bg-body`: `#F3F4F6` (Светло-серый + легкий шум/градиент).
    * `--bg-surface`: `#ffffff` (Карточки, сайдбар).
    * `--bg-glass`: `rgba(255, 255, 255, 0.8)` + `backdrop-filter: blur(12px)`.
* **Text:**
    * `--text-main`: `#111827` (Почти черный, для заголовков).
    * `--text-sec`: `#6B7280` (Серый, для подписей и иконок).
* **Status Colors:**
    * 🟢 Success: `#10B981` (Фон: `#ECFDF5`).
    * 🔴 Danger: `#EF4444` (Фон: `#FFF1F2`).
    * 🟠 Warning: `#F59E0B` (Фон: `#FFF7ED`).

### 🔡 Типографика
* **Основной шрифт:** `Inter` (UI, тексты, заголовки).
* **Моноширинный:** `JetBrains Mono` (Финансы, цены, время, артикулы).
    * *Важно:* Все цифры в дашбордах и таблицах должны быть моноширинными для удобства считывания.

### 📐 Формы и Эффекты
* **Border Radius:**
    * `--radius-xl`: `24px` (Карточки, Модальные окна).
    * `--radius-md`: `12px` (Кнопки, Инпуты).
* **Shadows:**
    * `--shadow-float`: Мягкая, глубокая тень для поднятых элементов (`0 20px 40px rgba...`).
    * `--shadow-glow`: Цветная тень для Primary кнопок.
* **Transitions:** `cubic-bezier(0.2, 0.8, 0.2, 1)` — быстрая, но плавная анимация.

---

## 2. Адаптивная Архитектура (Layout)
Приложение проектируется как **Mobile-First PWA**.

### 🖥️ Desktop (Width > 1024px)
* **Sidebar:** Фиксированная левая панель (`260px`).
* **Header:** Sticky (прилипающий) топ-бар с эффектом стекла.
* **Grid:** Bento-Grid (сетка виджетов), адаптирующаяся под ширину экрана.

### 📱 Mobile (Width < 1024px)
* **Viewport Fix:** Критически важно использовать `position: fixed` для `body` и `height: 100dvh`, чтобы избежать "скачков" интерфейса при скролле.
* **Navigation:** Сайдбар скрыт. Используется **Floating Bottom Nav** (парящая нижняя панель).
* **FAB:** Центральная кнопка действия («+») вынесена в Bottom Nav.
* **Modals:** Все модальные окна открываются как **Bottom Sheets** (шторки снизу).

---

## 3. Ключевые Компоненты (Component Library)

### 1. Smart Input (Даты и Числа)
* **Input Masking:** Автоматическое форматирование ввода.
    * `1.` -> `01.`
    * `0102` -> `01.02.`
* **Validation:** Проверка `onBlur`. Ошибки подсвечиваются красной рамкой и текстом снизу.

### 2. Карточки (Bento Widgets)
* **Интерактивность:** Карточки кликабельны. При наведении: `transform: translateY(-4px)` + усиление тени.
* **Visual Data:** Использование микро-графиков (Sparklines), прогресс-баров (CSS conic-gradient) и стеков аватаров.

### 3. Scheduler (График смен)
* **Структура:** CSS Grid.
    * Первая колонка (ФИО) — `position: sticky; left: 0`.
    * Заголовки (Дни) — `position: sticky; top: 0`.
* **Типы смен:**
    * *Active:* Сплошная заливка цветом роли.
    * *Draft:* Полосатый фон (repeating-linear-gradient), пунктирная рамка.
    * *Ghost:* Прозрачный фон, пунктир (для вакансий).
* **Live Metrics:** При изменении графика в реальном времени пересчитывается Labor Cost % (полоска сверху).

### 4. Smart Recipe Builder (ТТК)
* **Composition Bar:** Визуализация структуры себестоимости (цветная полоса под фото).
* **Drag & Drop:** Ингредиенты можно менять местами.
* **Live Calculator:** При изменении цены продажи мгновенно меняется цвет маржинальности (Зеленый/Красный).

---

## 4. Стек технологий (Рекомендации)

Для реализации такой высокой степени интерактивности рекомендуется:

* **Frontend:** React (Next.js) или Vue 3 (Nuxt).
* **Styling:** Tailwind CSS (идеально для дизайн-токенов) + Framer Motion (для сложных анимаций переходов и шторок).
* **Icons:** Lucide React / Heroicons (Outline стиль).
* **State Management:** Zustand / Pinia (для реактивных пересчетов калькуляторов).
* **Dates:** date-fns / Day.js.

---

## 5. Комментарий для разработчика

> Привет! Мы строим ERP нового поколения. Главный фокус — на **UX и визуализации**.
>
> 1. **Не используй нативные алерты и конфирмы.** Всё должно быть через красивые кастомные модалки или Toast-уведомления.
> 2. **Mobile Experience:** На телефоне это должно ощущаться как нативное приложение. Никакого зума, никакого горизонтального скролла всей страницы.
> 3. **Цифры — это важно.** Используй моноширинный шрифт везде, где есть деньги или остатки.

```
