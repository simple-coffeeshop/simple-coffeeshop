## 🚀 Спринт №1: Core & Identity (Фундамент системы)

**Цель:** Запуск мультитенантной архитектуры, системы безопасности и базового интерфейса в стиле Glassmorphism.

### 1. Архитектурное ядро (Backend & DB)

* **Strict Multi-Tenancy:** Реализация полной изоляции данных между «Бизнесами» на уровне БД. Каждая таблица (кроме глобальных справочников) получает обязательное поле `businessId`.
* **Prisma Schema:** Создание моделей `Business` (верхний уровень), `Enterprise` (юрлица), `Unit` (точки/склады) и `Member` (связь пользователя с бизнесом и ролью).
* **Temporal Engine:** Внедрение принципа **UTC Everywhere** для хранения времени и разработка **Timezone Helper** для корректного отображения времени точки в UI.

### 2. Безопасность и Доступ (Security Layer)

* **Magic Link Auth:** Реализация регистрации исключительно по одноразовым магическим ссылкам.
* **Auth:** Вход по логину и паролю
* **Ownership Handshake:** Механизм передачи прав владельца с обязательным 168-часовым кулдауном и функцией мгновенной отмены (Revocation) текущим владельцем.
* **Session Audit:** Интерфейс в профиле пользователя для мониторинга активных сессий и возможности удаленного выхода.
* **Allowed IP:** Настройка фильтрации доступа сотрудников по IP-адресу юнита.

### 3. Управление сетью (Module 00: Core Network)

* **Capabilities Logic:** Система динамического назначения прав юнитам (📦 Вести данные, 🔪 Готовить, 👥 Штат и т.д.), определяющая доступный функционал.
* **Unit Passport:** CRUD для юнитов с настройкой тегов (напр. `#Кофейня`, `#Собственная`) для фильтрации в других модулях.
* **Data Routing:** Матрица логистических связей `Child -> Category -> Parent` для маршрутизации внутренних заказов.

### 4. Визуальный язык и Layout (Frontend)

* **SCSS Foundation:** Настройка дизайн-токенов в `_variables.scss` (палитра Slate-950, Emerald-500) и системных миксинов для Glassmorphism (эффект матового стекла, блюр).
* **Command Palette (Cmd+K):** Реализация глобального поиска по юнитам и быстрых действий.
* **Bento Layout:** Создание основной сетки приложения с адаптивным Sidebar (Radial Highlight эффект) и Breadcrumbs (`Бизнес > Предприятие > Юнит`).

---

## Технический этап: Инициализация БД (Prisma)

```typescript
// prisma.config.ts - Обязательный конфиг
import { Prisma } from '@prisma/client';

export const prismaConfig: Prisma.PrismaClientOptions = {
  log: ['query', 'info', 'warn', 'error'],
  errorFormat: 'pretty',
};

/**
 * [EVAS_PROTIP]: В этом спринте мы внедряем middleware для Prisma, 
 * который автоматически добавляет `where: { businessId }` ко всем 
 * запросам, исключая утечку данных между клиентами.
 */

```
