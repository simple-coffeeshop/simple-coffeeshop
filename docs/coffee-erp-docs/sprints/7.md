### 🚀 Спринт №7: Assets & Public (Оборудование и Меню)

**Цель:** Управление жизненным циклом активов, автоматизация ремонтов и запуск публичного Digital Menu с мгновенной синхронизацией стоп-листов.

---

### Сравнение вариантов реализации Спринта №7

| Критерий | Хороший | Лучший | Оптимальный (Мой выбор) |
| --- | --- | --- | --- |
| **Связь с производством** | Ручное уведомление о поломке. | Пометка «Не работает» в списке. | **Equipment Safety Lock**: Автоматическая блокировка ТТК в Модуле 02 при статусе «Критическая авария». |
| **Публичное меню** | Статическая страница со списком. | PDF-файл для скачивания. | **Instant Menu Sync**: Мгновенное скрытие позиций при попадании ингредиентов в стоп-лист через Sync API. |
| **QR-доступ** | Ссылка на главную страницу. | Генерация одного кода на сеть. | **Dynamic Unit Routing**: Уникальные QR для каждой точки с роутингом на акции (завтраки/ланчи) по времени. |
| **Учет простоя** | Дата поломки в логах. | Календарь ремонтов. | **Downtime Tracker**: Живой счетчик простоя на карточке актива с авто-расчетом финансовых потерь. |

---

#### 1. Управление активами (Module 09: Asset Management)

* **Asset Passport & QR**: Создание цифровых паспортов оборудования с историей обслуживания, стоимостью владения и уникальными QR-кодами для быстрого вызова мастера.
* **Safety Lock Logic**: Программная связь между статусом оборудования и производством. Если кофемашина сломана, система блокирует возможность выбора связанных с ней ТТК во всех модулях.
* **Cross-Module Service Flow**: Автоматизация пути: Инцидент (Mod 08) → Сервисный тикет (Mod 09) → Расходная транзакция в кассе (Mod 05) при закрытии.
* **Maintenance Countdown**: Визуальный индикатор рабочих циклов или дней до следующего ТО (Предиктивное обслуживание).

#### 2. Гостевой сервис (Module 07: Guest Experience)

* **Digital Menu Engine**: Веб-интерфейс для гостей в стиле Glassmorphism, агрегирующий данные о составе, КБЖУ и аллергенах напрямую из техкарт.
* **Instant Stop-List Sync**: Механизм мгновенной инвалидации кэша публичного меню при обнулении остатков ингредиентов в Модуле 01.
* **Unit Switcher Loader**: Система динамического роутинга гостя по URL `/menu/:unit_slug` с плавной анимацией загрузки данных конкретной точки.
* **Marketing Promo Engine**: Инструмент размещения временных баннеров в меню с привязкой к календарю акций.

#### 3. Интерфейс (UI/UX)

* **Downtime Timer**: Тикающий счетчик времени восстановления на карточке актива в режиме реального времени.
* **Glassmorphism Guest UI**: Максимально легкий интерфейс на SCSS с эффектом блюра, оптимизированный под мобильные устройства без лишних библиотек.
* **QR Asset Inventory**: Специальный режим последовательного сканирования всех меток на точке для проведения быстрой инвентаризации имущества.

---

### Техническая реализация: Схема активов и публичного доступа

```typescript
// prisma/schema.prisma (дополнение)

model Asset {
  id              String   @id @default(cuid())
  name            String
  serialNumber    String?
  unitId          String   @index
  businessId      String   @index
  
  status          AssetStatus @default(OPERATIONAL)
  criticality     Int      @default(1) // Влияние на техкарты
  
  maintenanceDate DateTime?
  nextServiceIn   Int?     // Циклы или дни
  
  tickets         ServiceTicket[]
  createdAt       DateTime @default(now())
}

model ServiceTicket {
  id              String   @id @default(cuid())
  assetId         String
  asset           Asset    @relation(fields: [assetId], references: [id])
  
  description     String
  status          TicketStatus @default(OPEN)
  
  cost            Int?     // Фиксируется при закрытии в копейках
  downtimeMinutes Int?     // Рассчитывается при закрытии
  
  externalVendorId String?
  createdAt       DateTime @default(now())
}

enum AssetStatus {
  OPERATIONAL
  WARNING
  CRITICAL_FAILURE // Триггерит Safety Lock
  MAINTENANCE
  RETIRED
}

enum TicketStatus {
  OPEN
  IN_PROGRESS
  FIXED
  CLOSED
}

/**
 * [EVAS_PROTIP]: Для публичного меню мы используем Edge Caching 
 * с инвалидацией через Webhooks, чтобы гость получал меню 
 * мгновенно, но с актуальными стоп-листами.
 */

```
