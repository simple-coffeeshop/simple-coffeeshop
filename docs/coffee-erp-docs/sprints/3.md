## Сравнение вариантов реализации Спринта №3

| Критерий | Хороший | Лучший | Оптимальный (Мой выбор) |
| --- | --- | --- | --- |
| **Расчет себестоимости** | Ручной запуск пересчета при открытии техкарты. | Фоновое задание (Cron) раз в сутки. | **Реактивный Cascade Update**: мгновенный фоновый пересчет всей сети при изменении цены в Модуле 01. |
| **Редактор рецептов** | Обычное текстовое поле + список ID. | Выпадающий список ингредиентов. | **Smart Editor с токенами**: использование `$` или `@` для вставки интерактивных ссылок на сырье. |
| **Версионность** | Перезапись текущего файла. | Копирование старых строк в таблицу логов. | **JSON-Snapshot Versioning**: хранение полных слепков с поддержкой **Visual Diff** (подсветка изменений). |
| **Масштабирование** | Статические граммовки. | Пропорциональное умножение в БД. | **Batch Scaling UI**: пересчет «на лету» на стороне клиента с разделением цветов базового и расчетного веса. |

---

## 🚀 Спринт №3: Smart Production (Техкарты и Себестоимость)

**Цель:** Реализация реактивного ядра техкарт (ТТК) с автоматизацией финансовых и нутрициологических расчетов.

### 1. Движок техкарт (Logic & Database)

* **Hybrid Entity Logic**: Рецепт может быть как конечным блюдом, так и заготовкой (ингредиентом). Если отмечен как `Заготовка`, он автоматически становится доступен для выбора в других ТТК и для инвентаризации в Модуле 01.
* **Cascade Auto-COGS**: Разработка сервиса, который при обновлении `cost_price` сырья в Складе находит все связанные рецепты (включая вложенные) и пересчитывает их маржинальность.
* **Nutrition Engine**: Автоматический расчет КБЖУ готового блюда на основе данных ингредиентов с учетом коэффициента уварки/потерь.
* **Optimistic Locking**: Внедрение `version` поля в БД для предотвращения конфликтов при одновременном редактировании ТТК несколькими менеджерами.

### 2. Интеллектуальный интерфейс (UI/UX)

* **Smart Editor (Chips)**: Реализация floating panel поиска в редакторе. Символ `$` вставляет токен `Link(Ingredient_ID)`, визуализируемый как интерактивный Chip.
* **Visual Diff Visualizer**: Компонент для сравнения версий ТТК. Подсветка добавленных (зеленый) и удаленных (красный) строк рецепта.
* **Nutrition Progress Bars**: Визуализация БЖУ через кольцевые диаграммы и прогресс-бары в карточке товара.

### 3. Инструменты повара

* **Batch Scaling Preview**: Режим работы на планшете, позволяющий повару ввести нужный объем выхода и мгновенно получить пересчитанные граммовки ингредиентов.
* **Passive Alerts (Low Margin Glow)**: Если маржа рецепта падает ниже целевого порога (из-за роста цен), карточка в реестре получает пульсирующую красную рамку.

---

## Техническая реализация: Схема ТТК и Логика пересчета

Ниже представлена структура данных для обеспечения вложенности и версионности техкарт.

```typescript
// prisma/schema.prisma (дополнение)

model Recipe {
  id              String           @id @default(cuid())
  name            String
  category        String
  unitId          String           @index
  businessId      String           @index
  
  isSubRecipe     Boolean          @default(false) // Признак заготовки
  baseOutputAmount Float           // Базовый выход (напр. 1000 мл)
  lossPercentage  Float            @default(0)     // Потери при приготовлении
  
  targetMargin    Float            @default(60)    // Целевая маржа для алертов
  currentCogs     Int              @default(0)     // Текущая себестоимость в копейках
  
  ingredients     RecipeItem[]
  versions        RecipeVersion[]
  
  version         Int              @default(1)     // Для Optimistic Locking
  isArchived      Boolean          @default(false)
  updatedAt       DateTime         @updatedAt
}

model RecipeItem {
  id              String   @id @default(cuid())
  recipeId        String
  recipe          Recipe   @relation(fields: [recipeId], references: [id])
  
  // Может быть либо товаром, либо другим рецептом
  productId       String?
  subRecipeId     String?
  
  amount          Float    // Граммовка брутто
}

model RecipeVersion {
  id              String   @id @default(cuid())
  recipeId        String
  recipe          Recipe   @relation(fields: [recipeId], references: [id])
  snapshot        Json     // Полный слепок ТТК для Diff
  authorId        String
  createdAt       DateTime @default(now())
}

```

```typescript
// prisma.config.ts (обязательный файл)
import { Prisma } from '@prisma/client';

export const prismaConfig: Prisma.PrismaClientOptions = {
  log: ['error', 'warn'],
  errorFormat: 'pretty',
};

```
