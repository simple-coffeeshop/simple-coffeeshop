## 🚀 Спринт №6: Operational Trust (Контроль и Качество)

**Цель:** Цифровизация операционных стандартов, запуск системы контроля ХАССП (HACCP) и автоматизация аттестации персонала.

---

## Сравнение вариантов реализации Спринта №6

| Критерий | Хороший | Лучший | Оптимальный (Мой выбор) |
| --- | --- | --- | --- |
| **Фото-фиксация** | Обычная загрузка файлов из галереи. | Проверка времени снимка в приложении. | **True Capture & EXIF Validation**: Блокировка доступа к галерее и серверная сверка метаданных фото с GPS-координатами точки. |
| **Журналы ХАССП** | Текстовые поля для ввода температуры. | Валидация диапазонов «на лету». | **HACCP Visual Feedback**: Мгновенное окрашивание полей в `$color-danger` и форсирование создания инцидента при отклонении. |
| **Аттестация** | Список статических вопросов в PDF. | Тесты с выбором вариантов ответа. | **Auto-LMS Trigger**: Генерация тестов на основе реальных ТТК и автоматическое назначение пересдачи при частых инцидентах. |
| **Комплаенс** | Ручной контроль медкнижек. | Уведомление об истечении срока. | **Grace Period & Shift Gate**: Жесткая блокировка кнопки «Старт смены» при просрочке медкнижки (с учетом 3-дневного льготного периода). |

---

### 1. Операционный контроль (Module 08: Operational Control)

* **True Capture Engine**: Реализация системного запрета на использование галереи (только живое фото с камеры) для критических пунктов чек-листов.
* **Proof of Presence**: Автоматическое прикрепление GPS-меток к каждому фото-отчету и серверная сверка координат устройства с локацией Юнита.
* **HACCP Digital Journals**: Цифровые журналы температур и здоровья. При вводе данных вне нормы система активирует **HACCP Visual Feedback** и требует обязательный комментарий с описанием принятых мер.
* **Incident Log Integration**: Автоматическое создание тикета в Модуле 09 при отметке «Проблема» в операционном чек-листе.

### 2. Обучение и Аттестация (Module 06: LMS)

* **Certification Constructor**: Инструмент создания гибридных тестов (Теория через JSON-схему + Практика через офлайн-чеклисты для управляющих).
* **Contextual Knowledge Base**: Интеграция обучающих медиа-файлов прямо в интерфейс ТТК (Модуль 02) и Чек-листов для мгновенной подсказки.
* **Compliance Monitor**: Система мониторинга сроков медицинских книжек. Автоматическая блокировка доступа к работе через **Shift Gate Overlay** при критической просрочке.
* **Multi-Judge Interface**: Коллегиальная оценка навыков бариста с выявлением и визуализацией споров между проверяющими.

### 3. Интерфейс (UI/UX)

* **Shift Gate Overlay**: Полноэкранный блокировщик операционных функций для сотрудников без активной открытой смены.
* **Audit Score Dashboard**: Градиентные графики дисциплины (процент вовремя выполненных задач) для Дашборда Владельца.
* **Rose-500 Validation States**: Использование семантических переменных из `$color-danger` для индикации критических ошибок в журналах ХАССП.

---

## Техническая реализация: Схема контроля и квалификации

```typescript
// prisma/schema.prisma (дополнение)

model ChecklistResult {
  id              String   @id @default(cuid())
  templateId      String
  unitId          String
  authorId        String
  businessId      String   @index
  
  items           ChecklistItemResponse[]
  
  // Гео-позиционирование и верификация
  lat             Float?
  lng             Float?
  isVerified      Boolean  @default(false)
  
  status          ChecklistStatus @default(COMPLETED)
  createdAt       DateTime @default(now())
}

model Certification {
  id              String   @id @default(cuid())
  employeeId      String
  type            String   // THEORY, PRACTICE, HYBRID
  score           Float
  result          String   // PASSED, FAILED
  
  validUntil      DateTime?
  businessId      String   @index
  
  createdAt       DateTime @default(now())
}

model ComplianceRecord {
  id              String   @id @default(cuid())
  employeeId      String
  documentType    String   // e.g., "Medical Book"
  expiryDate      DateTime
  
  status          ComplianceStatus @default(VALID)
  gracePeriodUsed Boolean  @default(false)
  
  businessId      String   @index
}

enum ChecklistStatus {
  IN_PROGRESS
  COMPLETED
  FLAGGED // Если есть нарушения или аномалии в EXIF
}

enum ComplianceStatus {
  VALID
  WARNING
  EXPIRED
  BLOCKED
}

```
